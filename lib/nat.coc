; run with `npm run cli -- lib/nat.coc`

let Nat (/ t * -> t (-> t t) t)
let z (: (\(t z s) z) Nat )
let s (: (\(n t z s) s (n t z s)) (-> Nat Nat))
let add (: (\(n m) n Nat m s) (-> Nat Nat Nat))

let main (add (s z) (s (s z)))
